/**
 * ADMIN CONFIGURATION
 * ===================
 *
 * Authentication is now handled via data/users.json (bcrypt hashed passwords).
 *
 * To manage users:
 * 1. Log in to /admin with your current credentials
 * 2. Go to the "Users" tab
 * 3. Add, edit, or delete users from there
 *
 * Recovery email: andy@panafricatelecom.co.za
 * All password resets are sent to this address.
 *
 * The old hardcoded credentials (admin / patadmin2024) still work
 * and are stored in data/users.json.
 */

export const ADMIN_SITE_TITLE = "Pan-Africa Telecom Admin";
export const RECOVERY_EMAIL = "andy@panafricatelecom.co.za";